mixin filterButtons(filterButtons) 
    each item in filterButtons
        button(
            class=`btn btn-sm ml-1 btn-outline-success ${item.class}`
            status-data=item.status
        ) #{item.name}