extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/product-layout.pug

block main 
    
    .row    
        .col-12.d-flex.justify-content-center.mt-3
            .chat 
                .inner-body
                    each chat in chats 
                            div(class=(chat.user_id != user.id ? "inner-incomming" : "inner-outgoing d-flex justify-content-end"))
                                if(chat.user_id != user.id)
                                    .inner-name #{chat.userInfor.fullName}
                                .inner-content #{chat.content}
                            
                    div(class='inner-list-typing')
                        div(class='box-typing')
                            div(class='inner-name') Le Van A 
                            div(class='inner-dots') 
                                span 
                                span 
                                span 
                        
                        div(class='box-typing')
                            div(class='inner-name') Le Van A 
                            div(class='inner-dots') 
                                span 
                                span 
                                span 
                    
                .inner-foot 
                    form(
                        class='inner-form'
                        action=''
                    )
                       
                        input(
                            text=''
                            placeholder='Type here ...'
                            name='content'
                        )
                        button(class='button-icon') 🐔
                        button Send

    div(class='tooltip' role='tooltip')
        emoji-picker

        